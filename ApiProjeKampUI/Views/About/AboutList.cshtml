@model List<ResultAboutDto>

@{
    ViewData["Title"] = "AboutList";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}


<section class="section">
    <div class="section-body">
        <div class="row">
            <div class="col-12 col-md-12 col-lg-12">
                <div class="card">
                    <div class="card-header">
                        <h4>Hakkımda Sayfası Paneli</h4>
                    </div>
                    <div class="card-body">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th scope="col">Başlık</th>
                                    <th scope="col">Açıklama</th>
                                    <th scope="col">Sil</th>
                                    <th scope="col">Güncelle</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <th scope="row">@item.Title</th>
                                        <td>@item.Description</td>
                                        <td><a href="/About/AboutDelete/@item.AboutId/" class="btn btn-danger">Sil</a></td>
                                        <td><a href="/About/AboutUpdate/@item.AboutId/" class="btn btn-warning">Güncelle</a></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <td><a href="/About/AboutCreate/" class="btn btn-secondary">Ekle</a></td>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
